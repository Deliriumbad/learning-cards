import RoutesPath from '../../../routes/RoutesPath';

import Header from './Header/Header';

const Main = () => {
    return (
        <div>
            <Header />
            <RoutesPath />
        </div>
    );
};
export default Main;
