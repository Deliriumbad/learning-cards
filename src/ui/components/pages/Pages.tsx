import RoutesPath from '../../../routes/RoutesPath';

import Header from './Header/Header';

const Pages = () => {
    return (
        <div>
            <Header />
            <RoutesPath />
        </div>
    );
};
export default Pages;
